parameters:
    git_dir: .
    bin_dir: ./bin
    tasks:
        phpcs:
            standard: ./phpcs.xml
            whitelist_patterns:
                - /^src\/(.*)/
                - /^tests\/(.*)/
